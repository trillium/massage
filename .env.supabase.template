# Supabase Configuration
# Get these values from: https://app.supabase.com/project/_/settings/api

# Your Supabase project URL (safe to expose in browser)
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co

# Your Supabase anon/public key (safe to expose in browser)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Your Supabase service role key (SECRET - never expose to browser!)
# Only use this in server-side code, API routes, or server components
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

## Cookie Domain (for cross-subdomain authentication)
# Leave empty/commented for localhost development
# Set to .yourdomain.com for production (leading dot = all subdomains)
# COOKIE_DOMAIN=.yourdomain.com

# --- KEEP YOUR EXISTING ENVIRONMENT VARIABLES BELOW ---
# Google OAuth (existing)
GOOGLE_OAUTH_REFRESH=
GOOGLE_OAUTH_CLIENT_ID=
GOOGLE_OAUTH_SECRET=

# Email & Contact (existing)
OWNER_EMAIL=
OWNER_PHONE_NUMBER=
ADMIN_EMAILS=

# PostHog Analytics (existing)
NEXT_PUBLIC_POSTHOG_KEY=
NEXT_PUBLIC_POSTHOG_HOST=

# API Security (existing)
UPDATE_LOC_PASSWORD=
