import type { RawMonthData } from './changelog.types'

const data: RawMonthData = {
  date: '2025-06',
  repos: ["massage"],
  authorNotes: '',
  commitCount: 1,
  commits: [
    'test(vitest): migrate all tests from Jest to Vitest, update mocks and type usage',
  ],
}

export default data
