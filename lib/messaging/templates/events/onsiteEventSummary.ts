/**
 * Creates a title "summary" for an onsite calendar event.
 *
 * @function
 * @returns {string} Returns the summary string for an onsite event.
 */
function onsiteEventSummary({ eventContainerString }: { eventContainerString: string }) {
  return eventContainerString
}

export default onsiteEventSummary
